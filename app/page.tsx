"use client";
import {useMemo,useState} from "react";
type Tx={date:string,description:string,amount:number,category:string};
const rules:[RegExp,string][]=[[/netflix|spotify|apple\.?com|youtube premium|google|amazon prime|disney|stan|binge|audible|canva|adobe|microsoft|dropbox/i,"Subscriptions"],[/uber eats|doordash|menulog|deliveroo/i,"Delivery"],[/cafe|coffee|starbucks|mcdonald|kfc|restaurant|grill|pizza|bakery|sushi|burger/i,"Eating out"],[/uber(?! eats)|didi|taxi/i,"Transport"]];
const money=(n:number)=>new Intl.NumberFormat("en-AU",{style:"currency",currency:"AUD"}).format(n);
function analyseLine(line:string):Tx|null{const hit=rules.find(([r])=>r.test(line));if(!hit)return null;const nums=[...line.matchAll(/(?:\$\s*)?(-?\d{1,3}(?:,\d{3})*\.\d{2}|-?\d+\.\d{2})/g)].map(m=>m[1]);if(!nums.length)return null;const amount=Math.abs(Number(nums[0].replace(/,/g,"")));if(!amount||!isFinite(amount)||amount>100000)return null;const date=(line.match(/\b(?:\d{1,2}[\/\-.]\d{1,2}(?:[\/\-.]\d{2,4})?|\d{1,2}\s+(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)[a-z]*\s*\d{0,4})\b/i)||[])[0]||"";return {date,description:line.replace(/\s+/g," ").trim(),amount,category:hit[1]}}
function analyseText(text:string){const out:Tx[]=[];for(const line of text.split(/\r?\n/).slice(0,10000)){const t=analyseLine(line);if(t)out.push(t)}return out}
async function pdfText(file:File){const pdfjs=await import("pdfjs-dist/legacy/build/pdf.mjs");pdfjs.GlobalWorkerOptions.workerSrc="https://unpkg.com/pdfjs-dist@5.4.149/legacy/build/pdf.worker.min.mjs";const pdf=await pdfjs.getDocument({data:new Uint8Array(await file.arrayBuffer())}).promise;let text="";for(let p=1;p<=pdf.numPages;p++){const page=await pdf.getPage(p);const c=await page.getTextContent();let line="";let lastY:number|null=null;for(const item of c.items as any[]){if(!("str" in item))continue;const y=item.transform?.[5]??0;if(lastY!==null&&Math.abs(y-lastY)>2){text+=line+"\n";line=""}line+=(line?" ":"")+item.str;lastY=y}text+=line+"\n"}return text}
export default function Home(){const [tx,setTx]=useState<Tx[]>([]);const [status,setStatus]=useState("");const [busy,setBusy]=useState(false);const parse=async(files:FileList)=>{setBusy(true);setStatus("Reading your statements…");let out:Tx[]=[];let failed=0;for(const f of Array.from(files)){try{const isPdf=f.type==="application/pdf"||f.name.toLowerCase().endsWith(".pdf");const text=isPdf?await pdfText(f):await f.text();out=out.concat(analyseText(text))}catch{failed++}}const seen=new Set<string>();out=out.filter(t=>{const k=t.date+"|"+t.description+"|"+t.amount;if(seen.has(k))return false;seen.add(k);return true});setTx(out);setBusy(false);setStatus(out.length?`Analysed ${files.length} statement${files.length>1?"s":""} · found ${out.length} flagged transactions.`:failed===files.length?"We couldn't read that statement. Try a text-based PDF or CSV.":"No subscription or discretionary transactions were detected.")};const total=tx.reduce((a,b)=>a+b.amount,0);const groups=useMemo(()=>Object.entries(tx.reduce((a,t)=>{a[t.category]=(a[t.category]||0)+t.amount;return a},{} as Record<string,number>)).sort((a,b)=>b[1]-a[1]),[tx]);return <main><header><b>Spend Wise</b><span>Private by design</span></header><section className="hero"><div className="eyebrow">YOUR MONEY, MADE CLEAR</div><h1>See where your<br/>money really goes.</h1><p>Upload bank statements and instantly spot subscriptions, delivery, eating out and other spending you may want to cut.</p><label className="upload">{busy?"Analysing…":"Upload statements"}<input disabled={busy} type="file" multiple accept=".pdf,.csv,application/pdf,text/csv,text/comma-separated-values" onChange={e=>e.target.files&&parse(e.target.files)}/></label><small>PDF + CSV · No bank login · Analysis happens on your device</small>{status&&<p style={{fontSize:14,marginTop:18}}>{status}</p>}</section>{tx.length>0&&<section className="dash"><div className="top"><div><span>Flagged spending</span><strong>{money(total)}</strong><em>across {tx.length} transactions</em></div><div className="ring">{tx.length}</div></div><h2>Your spending breakdown</h2><div className="grid">{groups.map(([k,v])=><article key={k}><i>{k==="Subscriptions"?"▣":k==="Delivery"?"◆":k==="Eating out"?"●":"↗"}</i><span>{k}</span><b>{money(v)}</b><small>{Math.round(v/total*100)}% of flagged spend</small></article>)}</div><h2>Flagged purchases</h2><div className="list">{tx.slice(0,50).map((t,i)=><div key={i}><span><b>{t.description.slice(0,60)}</b><small>{t.category}{t.date?" · "+t.date:""}</small></span><strong>{money(t.amount)}</strong></div>)}</div></section>}{!tx.length&&!busy&&<section className="features"><h2>Find the leaks without linking your bank.</h2><div><article><b>01</b><h3>Upload</h3><p>Select PDF or CSV bank statements straight from Files. Upload several months at once.</p></article><article><b>02</b><h3>Understand</h3><p>Spend Wise reads the transactions and groups common discretionary spending.</p></article><article><b>03</b><h3>Save</h3><p>See what is adding up and decide what is actually worth keeping.</p></article></div></section>}<footer><b>Spend Wise</b><span>Built for clarity, not judgement.</span></footer></main>}